export enum RoleType {
    user = "user",
    botReply = "botReply", // when a bot replies to a user //// for conversation 
    botAuto = "botAuto", // when a bot sends an automated message  //// for conversation 
    bot = "bot" // just for message .. 
}